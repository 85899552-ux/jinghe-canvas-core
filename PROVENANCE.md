# 来源与提取记录

此候选包来自维护者指定的本地“剧本画布”项目。维护者王杰已在本次准备过程中确认：其为主要维护者，并有权公开自己的代码。

## 代码范围

- `src/lib/canvasNodeConnections.ts`
- `src/lib/imagePromptReferences.ts`
- `src/lib/canvasMinimap.ts`
- `src/lib/canvasPointerGesture.ts`
- `src/lib/promptMentions.ts`
- `src/types/canvas.ts`

以上逻辑模块仅将原应用的 `@/types/canvas` 类型路径替换为可独立解析的 `../types/canvas.ts`。类型文件保持原样。节点数量上限、行为和算法保持原实现。

新增：包元数据、TypeScript配置、命名空间导出入口、运行示例和说明文档。

## 测试范围

保留上述五个逻辑模块对应的原有测试。原连线测试文件末尾有一项只读取完整应用组件并匹配源码的集成检查；因完整应用组件不在本包范围，未将该检查复制入包。其余原有测试保持原样。

本包不包含完整画布的视觉及端到端验证。独立包测试通过不能证明原应用或模型提供方可用。

## 未纳入内容

原项目中的界面组件及样式、Lovart下载素材、第三方品牌资源、环境文件、真实媒体、业务数据库、日志、归档和模型服务凭据均未纳入。

这是一份来源说明，不取代第三方权利审查或正式开源许可证。
